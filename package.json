{
  "name": "solar-system-explorer",
  "version": "0.1.0",
  "description": "A photorealistic, first-person 3D explorer of the solar system. Jupiter system first.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy dist",
    "deploy:preview": "npm run build && wrangler pages deploy dist --branch preview"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "latest",
    "vite": "^6.3.5",
    "wrangler": "latest"
  },
  "dependencies": {
    "postprocessing": "^6.39.2",
    "three": "^0.185.1"
  }
}
